Module LTE-Sampler-Residential.src.utils.hpxml.ParseHPXMLinputs
===============================================================

Functions
---------

`parse_xml_file(file_path)`
:   Parse the XML file and return the root element.
    
    Args:
            file_path (str): Path to the XML file.
    
    Returns:
            Element: Root element of the parsed XML file.

`parse_xml_string(xml_string)`
:   Parse an XML string and return the root element.
    
    Args:
            xml_string (str): XML content as a string.
    
    Returns:
            Element: Root element of the parsed XML string.

`print_argument_fromfile(file_path)`
:   

`save_argument_fromfile(file_path, output_file)`
: